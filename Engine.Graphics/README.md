# Engine.Graphics

This assembly provides an API-agnostic graphics abstraction (`IGraphicsDevice`, swapchain, frame contexts) with a Vulkan backend via `GraphicsDevice`.

More detailed lifecycle and resource documentation will be added as the implementation evolves.

